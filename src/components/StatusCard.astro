---
// 状态卡片组件 - 显示一些有趣的状态信息
---

<div class="space-y-4">
  <h3 class="text-xl font-semibold text-white/90 flex items-center gap-2">
    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
    </svg>
    状态
  </h3>
  
  <div class="grid grid-cols-2 gap-3">
    <!-- 状态项 -->
    <div class="status-item bg-white/5 rounded-lg p-3 border border-white/10 hover:border-green-400/50 transition-all">
      <div class="flex items-center gap-2 mb-1">
        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
        <span class="text-xs text-gray-400">状态</span>
      </div>
      <span class="text-white font-medium">在线 🐱</span>
    </div>
    
    <div class="status-item bg-white/5 rounded-lg p-3 border border-white/10 hover:border-blue-400/50 transition-all">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-3 h-3 text-blue-400" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
        </svg>
        <span class="text-xs text-gray-400">心情</span>
      </div>
      <span class="text-white font-medium">喵喵喵！</span>
    </div>
    
    <div class="status-item bg-white/5 rounded-lg p-3 border border-white/10 hover:border-purple-400/50 transition-all">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-3 h-3 text-purple-400" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
        </svg>
        <span class="text-xs text-gray-400">爱好</span>
      </div>
      <span class="text-white font-medium">折腾小鸡</span>
    </div>
    
    <div class="status-item bg-white/5 rounded-lg p-3 border border-white/10 hover:border-yellow-400/50 transition-all">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-3 h-3 text-yellow-400" fill="currentColor" viewBox="0 0 24 24">
          <path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zm4.24 16L12 15.45 7.77 18l1.12-4.81-3.73-3.23 4.92-.42L12 5l1.92 4.53 4.92.42-3.73 3.23L16.23 18z"/>
        </svg>
        <span class="text-xs text-gray-400">座右铭</span>
      </div>
      <span class="text-white font-medium text-sm">少点404</span>
    </div>
  </div>
  
  <!-- 访问统计 -->
  <div class="mt-4 pt-4 border-t border-white/10">
    <div class="flex items-center justify-between text-sm">
      <span class="text-gray-400">今日访问</span>
      <span id="visit-count" class="text-white font-mono counter">---</span>
    </div>
    <div class="flex items-center justify-between text-sm mt-2">
      <span class="text-gray-400">运行时间</span>
      <span id="uptime" class="text-white font-mono">---</span>
    </div>
  </div>
</div>

<style>
  .status-item {
    transition: all 0.3s ease;
  }
  
  .status-item:hover {
    transform: translateY(-2px);
    background: rgba(255, 255, 255, 0.08);
  }
  
  .counter {
    display: inline-block;
    min-width: 50px;
    text-align: right;
  }
</style>

<script>
  // 模拟访问计数动画
  function animateCounter(element: HTMLElement, target: number, duration: number = 2000) {
    const start = 0;
    const increment = target / (duration / 16);
    let current = start;
    
    const timer = setInterval(() => {
      current += increment;
      if (current >= target) {
        current = target;
        clearInterval(timer);
      }
      element.textContent = Math.floor(current).toString();
    }, 16);
  }
  
  // 计算运行时间
  function calculateUptime() {
    // 假设网站上线日期
    const launchDate = new Date('2024-01-01');
    const now = new Date();
    const diff = now.getTime() - launchDate.getTime();
    
    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    
    return `${days}天 ${hours}小时`;
  }
  
  // 初始化
  const visitEl = document.getElementById('visit-count');
  const uptimeEl = document.getElementById('uptime');
  
  if (visitEl) {
    // 随机访问数 (模拟)
    animateCounter(visitEl, Math.floor(Math.random() * 500) + 100);
  }
  
  if (uptimeEl) {
    uptimeEl.textContent = calculateUptime();
  }
</script>
